# Local LLM Grammar Issue

Hand-written GBNF grammar can reduce malformed JSON when llama.cpp performs structured extraction.

The validator remains the trust gate because grammar-constrained output can still cite the wrong quote.

